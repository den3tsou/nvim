* Put this folder under ~/.config
* iterm2 is needed for Mac due to True Colour issue
* install Go and node
  + Go to golnag website to install
  + use `curl -L https://bit.ly/n-install | bash` to install
* nvim ./lua/den/packer.lua and run `:so`
